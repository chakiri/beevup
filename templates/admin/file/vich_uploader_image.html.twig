<a href="{{ vich_uploader_asset(item, 'kbisFile') }}" class="easyadmin-thumbnail" data-featherlight="#easyadmin-lightbox-{{ item.id }}" data-featherlight-close-on-click="anywhere">
    {# the second parameter is the name of the property with the UploadableField annotation #}
    {#<img src="{{ vich_uploader_asset(item, 'kbisFile') }}">#}
    <embed src="{{ vich_uploader_asset(item, 'kbisFile') }}" width="400" height="200">
</a>

<div id="easyadmin-lightbox-{{ item.id }}" class="easyadmin-lightbox">
    {# the second parameter is the name of the property with the UploadableField annotation #}
    {#<img src="{{ vich_uploader_asset(item, 'kbisFile') }}">#}
    <embed src="{{ vich_uploader_asset(item, 'kbisFile') }}" width="400" height="200">
</div>