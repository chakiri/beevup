<div class="modal-header">
    <h5 class="modal-title">Validation du Kbis</h5>
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
    </button>
</div>
<div class="modal-body">
    <iframe src="{{ vich_uploader_asset(item, 'kbisFile') }}" width="100%" height="100%"></iframe>
</div>
<div class="modal-footer">
    <a href="{{ path('valid_kbis', {'label': item.id}) }}" class="btn btn-success">Valider ce Kbis</a>
    <a href="{{ path('reject_kbis', {'label': item.id}) }}" class="btn btn-warning">Refuser ce Kbis</a>
</div>