<div class="row box dashboard-box">
    <div  class="box-title-container">
        <p class="box-title">Conseiller</p>
    </div>
    <div class="adviser-bloc-description">
        Nihil morati post haec militares avidi saepe turbarum adorti sunt Montium
    </div>
    <div class="avatar">
        {% if adviser.profile.filename %}
            <img src="{{ vich_uploader_asset(adviser.profile, 'imageFile') }}" class="img-fluid" alt="">
        {% else %}
            <img src="/images/default/user.png" class="img-fluid dashboard-profil-img" alt="">
        {% endif %}
    </div>
    <div class="info mt-3">
        <p class="text-muted user-name">{{ adviser.profile.lastname|capitalize ?: 'Profil incomplet'}} {{ adviser.profile.firstname|capitalize }}</p>
        <p class="user-contact">
            <span class="user-action btn-radius"><a class="user-link" href="{{ path('chat_private', {'id': adviser.id}) }}">Contacter</a></span>
            <span class="user-action btn-radius"><a class="user-link" href="{{path('profile_show', { 'id': adviser.profile.id}) }}">Voir fiche</a></span>
        </p>
    </div>
</div>