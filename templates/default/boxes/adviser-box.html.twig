<div class="row box dashboard-box">
    <div  class="col-12 box-title-container">
        <p class="box-title">Conseiller</p>
    </div>
    <div class="adviser-bloc-description">
        Nihil morati post haec militares avidi saepe turbarum adorti sunt Montium
    </div>
    <div class="avatar">
        {% if app.user.profile.filename %}
            <img src="{{ vich_uploader_asset(app.user.profile, 'imageFile') }}" class="img-fluid" alt="">
        {% else %}
            <img src="/images/default/user.png" class="img-fluid dashboard-profil-img" alt="">
        {% endif %}
    </div>
    <div class="info mt-3">
        <p class="text-muted user-name">{{ app.user.profile.lastname|capitalize ?: 'Profil incomplet'}} {{ app.user.profile.firstname|capitalize }}</p>
        <p class="user-contact">
            <span class="user-action btn-radius"><a class="user-link" href="{{ path('chat_private', {'id': app.user.id}) }}">Contacter</a></span>
            <span class="user-action btn-radius"><a class="user-link" href="{{path('profile_show', { 'id': app.user.profile.id}) }}">Voir fiche</a></span>
        </p>
    </div>
</div>