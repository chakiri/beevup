<div class="mt-3">
    <small>Offre exclusif</small>
    <div class="box-image">
        <a  href="{{ path('service_discovery') }}">
            {% if lastSpecialOffer.filename %}
                <img src="{{ vich_uploader_asset(lastSpecialOffer, 'imageFile') }}" class="img-fluid" alt="" style="border-radius:5px">
            {% else %}
                <img src="/images/no-available-photo.png" class="img-fluid" alt="" style="border-radius:5px">
            {% endif %}
        </a>
    </div>
    <p>{{ lastSpecialOffer.title }}</p>
    <a href="{{ path('service_discovery') }}">Découvrir</a>
</div>