{% embed 'default/modals/base.html.twig' with {id: 1} %}

    {% block header %}
        <div class="modal-header">
            <img src="/images/logo_beevup_solo.png" alt=""/>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    {% endblock %}

    {% block body %}
        <div class="row">
            <div class="col-3">
                <img class="logo" src="/images/logo_bv_main.png" alt=""/>
            </div>
            <div class="col-9">
                <p>Bienvenue sur la plateforme Beev’Up, le nouveau service proposé par {{ adminStore.profile.firstname }} {{ adminStore.profile.lastname }} et son équipe du magasin Bureau Vallée de {{ app.user.store.city }}</p>
            </div>
        </div>
    {% endblock %}

    {% block footer %}
        <div class="button">
            <button type="button" class="btn btn-primary next" id="discovery">Decouvrir</button>
        </div>
    {% endblock %}

{% endembed %}